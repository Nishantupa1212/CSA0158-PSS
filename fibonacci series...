#include<stdio.h>
int fib(int n)
{
	if(n<=1)
	return n;
	return fib(n-1)+fib(n-2);	
}
int main()
{
	int num,n=0,i;
	printf("Enter the number of terns =");
	scanf("%d",&num);
	printf("Facb series");
	for(i=0;i<num;i++)
	{
		printf("%d",fib(n));
		printf("\n");
		n++;
	}
}
